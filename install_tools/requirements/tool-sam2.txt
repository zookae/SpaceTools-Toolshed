# Requirements for SAM2 Segmentation tool environment
# Install in a dedicated conda environment with matching Python and Ray versions
#
# Setup:
#   source scripts/setup_tool_env.sh tool_sam2 sam2
#
# Or manually:
#   conda create -n tool_sam2 python==3.11 -y
#   conda activate tool_sam2
#   pip install ray==2.47.1
#   pip install -e .
#   pip install -r requirements/tool-sam2.txt

# PyTorch (install first, may need CUDA-specific version)
torch

# SAM2 from GitHub (not yet on PyPI)
sam-2 @ git+https://github.com/facebookresearch/sam2.git

# Model loading and configuration
huggingface_hub>=0.17.0
hydra-core>=1.3
omegaconf>=2.3

# Vision dependencies
pillow>=9.0.0
numpy>=2.0.0
matplotlib>=3.5.0
scipy>=1.10.0
opencv-python



